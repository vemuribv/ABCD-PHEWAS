---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/abcd_phewas/__init__.py
  - src/abcd_phewas/config.py
  - src/abcd_phewas/loader.py
  - src/abcd_phewas/type_detector.py
  - tests/conftest.py
  - tests/test_loader.py
  - tests/test_type_detector.py
  - config/domain_mapping.yaml
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Loading a cluster CSV and phenotype CSV produces a merged DataFrame with only subjects in both files"
    - "Sentinel values (-999, 777, 999) are replaced with NaN before any other processing"
    - "CRLI blocklist variables are dropped immediately after merge"
    - "Missingness rates are computed after sentinel replacement"
    - "Variables with <10 non-missing subjects in any group are flagged for exclusion"
    - "Every phenotype column is classified as binary, ordinal, categorical, or continuous"
    - "Override CSV overrides auto-detected types"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, pytest config"
      contains: "abcd_phewas"
    - path: "src/abcd_phewas/config.py"
      provides: "Dataclass config with all configurable parameters"
      exports: ["PipelineConfig"]
    - path: "src/abcd_phewas/loader.py"
      provides: "CSV loading, merging, sentinel replacement, CRLI blocking, missingness"
      exports: ["load_and_merge", "replace_sentinels", "apply_crli_blocklist", "compute_missingness", "has_enough_data"]
    - path: "src/abcd_phewas/type_detector.py"
      provides: "Variable type auto-detection with override"
      exports: ["VarType", "detect_type", "detect_all_types", "apply_overrides"]
    - path: "config/domain_mapping.yaml"
      provides: "8-domain regex config extracted from R codebase"
      contains: "Cognition"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: synthetic DataFrame with known types, sentinels, clusters"
    - path: "tests/test_loader.py"
      provides: "Tests for DATA-01, DATA-03, DATA-04"
    - path: "tests/test_type_detector.py"
      provides: "Tests for DATA-02"
  key_links:
    - from: "src/abcd_phewas/loader.py"
      to: "src/abcd_phewas/config.py"
      via: "PipelineConfig dataclass"
      pattern: "from abcd_phewas.config import"
    - from: "src/abcd_phewas/type_detector.py"
      to: "src/abcd_phewas/loader.py"
      via: "Runs after sentinel replacement on merged DataFrame"
      pattern: "VarType"
---

<objective>
Scaffold the Python project and implement the data loading + variable type detection pipeline.

Purpose: Create the foundational package structure, configuration, and the two core modules (loader, type_detector) that every downstream component depends on. After this plan, data can be loaded, cleaned, merged, and typed.

Output: Working `abcd_phewas` package with loader and type_detector modules, full test coverage, and the domain_mapping.yaml config.
</objective>

<execution_context>
@/Users/bhargav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhargav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold project and implement loader module</name>
  <files>pyproject.toml, src/abcd_phewas/__init__.py, src/abcd_phewas/config.py, src/abcd_phewas/loader.py, config/domain_mapping.yaml, tests/conftest.py, tests/test_loader.py</files>
  <behavior>
    - test_inner_merge: Merging cluster df (subjects A,B,C) with pheno df (subjects B,C,D) keeps only B,C
    - test_configurable_cols: Loading with subject_col="ID" and cluster_col="group" uses those column names
    - test_sentinel_replacement: DataFrame with values [-999, 777, 999, 5, 10] has sentinels replaced with NaN, leaving 5 and 10 intact
    - test_sentinel_before_type_detection: A column with values (-999, 0, 1) after sentinel removal has 2 unique values (binary), not 3
    - test_missingness_rate: After sentinel replacement, missingness rate = count_of_NaN / total_rows per column
    - test_crli_blocklist: Columns listed in blocklist file are dropped; unlisted columns remain
    - test_min_n_filter: Variable with 8 non-missing values in one group returns has_enough_data=False; variable with 15 in all groups returns True
  </behavior>
  <action>
    1. Create pyproject.toml with project metadata, dependencies (pandas>=2.0, numpy>=1.24, scipy>=1.10, pyyaml>=6.0, loguru>=0.7), dev dependencies (pytest>=7.4, pytest-cov>=4.0), and pytest config (testpaths=["tests"], pythonpath=["src"]).

    2. Create src/abcd_phewas/__init__.py with package version.

    3. Create src/abcd_phewas/config.py with a PipelineConfig dataclass:
       - cluster_path: str
       - phenotype_path: str
       - subject_col: str = "src_subject_id"
       - cluster_col: str = "cluster"
       - blocklist_path: str | None = None
       - override_path: str | None = None
       - domain_config_path: str = "config/domain_mapping.yaml"
       - sentinels: list[int | float] = field(default_factory=lambda: [-999, 777, 999])
       - min_n_per_group: int = 10
       - skew_threshold: float = 1.96
       - winsor_n_sd: float = 3.0

    4. Create src/abcd_phewas/loader.py with functions:
       - load_and_merge(config: PipelineConfig) -> pd.DataFrame: Read cluster CSV (usecols=[subject_col, cluster_col]) and phenotype CSV, inner merge on subject_col.
       - replace_sentinels(df: pd.DataFrame, sentinels: list, subject_col: str, cluster_col: str) -> pd.DataFrame: Replace sentinel values with np.nan on all columns EXCEPT subject_col and cluster_col. Use df[pheno_cols].replace(sentinels, np.nan) pattern.
       - apply_crli_blocklist(df: pd.DataFrame, blocklist_path: str | None) -> pd.DataFrame: Read blocklist file (one var per line), drop matching columns. Log dropped count.
       - compute_missingness(df: pd.DataFrame, pheno_cols: list[str]) -> pd.DataFrame: Return DataFrame with columns [variable, missingness_rate, n_missing, n_total].
       - has_enough_data(series: pd.Series, groups: pd.Series, min_n: int = 10) -> bool: Check every group has >= min_n non-missing observations.
       - get_pheno_cols(df: pd.DataFrame, subject_col: str, cluster_col: str) -> list[str]: Return all columns except subject_col and cluster_col.

    5. Create config/domain_mapping.yaml with the 8-domain structure from RESEARCH.md (Cognition, Screen Time, Demographics, Substance, Culture/Environment, Physical Health, Family Mental Health, Child Mental Health, plus Other/Unclassified fallback). Use the exact regex patterns and color values from RESEARCH.md.

    6. Create tests/conftest.py with shared fixtures:
       - sample_cluster_df: 20 subjects (sub-01 to sub-20), cluster labels 1-3
       - sample_pheno_df: 15 subjects (sub-05 to sub-19, overlapping 15 with cluster df), 10 columns of mixed types:
         * 1 binary column (values 0, 1)
         * 1 ordinal column (values 1,2,3,4,5)
         * 1 categorical column (values "A","B","C","D")
         * 5 continuous columns (random normal, >10 unique)
         * 1 column with sentinel values (-999 mixed in)
         * 1 column with very few non-missing values (for min_n test)
       - tmp_csv_files fixture that writes cluster and pheno dfs to temp CSV files
       - tmp_blocklist fixture that writes a blocklist file

    7. Create tests/test_loader.py with the behavior tests listed above. Run RED first (tests fail), then implement, then GREEN.
  </action>
  <verify>
    <automated>cd /Users/bhargav/ai-coding/ABCD-PHEWAS && pip install -e ".[dev]" && pytest tests/test_loader.py -x -v</automated>
  </verify>
  <done>All loader tests pass. Package installs cleanly. Config dataclass has all fields. Domain mapping YAML has 8 domains with regex patterns and colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement variable type detection module</name>
  <files>src/abcd_phewas/type_detector.py, tests/test_type_detector.py</files>
  <behavior>
    - test_binary: Column with exactly 2 unique non-NA values (0, 1) -> VarType.BINARY
    - test_ordinal: Column with values (1,2,3,4,5) sequential integers -> VarType.ORDINAL
    - test_ordinal_as_float: Column with values (1.0, 2.0, 3.0) stored as float -> VarType.ORDINAL (not categorical)
    - test_categorical: Column with values ("A","B","C","D") non-sequential -> VarType.CATEGORICAL
    - test_categorical_nonsequential_ints: Column with values (1,3,5,7) non-sequential ints -> VarType.CATEGORICAL
    - test_continuous: Column with >10 unique values -> VarType.CONTINUOUS
    - test_all_na: Column with all NaN -> VarType.CONTINUOUS (default)
    - test_override: Auto-detected as categorical, override CSV says continuous -> result is continuous
    - test_detect_all_types: DataFrame with mixed columns returns correct dict mapping
    - test_binary_after_sentinel: Column with values (-999, 0, 1) after sentinel removal has 2 unique values -> BINARY (validate integration with sentinel step)
  </behavior>
  <action>
    1. Create src/abcd_phewas/type_detector.py with:
       - VarType(str, Enum): BINARY, ORDINAL, CATEGORICAL, CONTINUOUS
       - UNIQUE_THRESHOLD = 10
       - detect_type(series: pd.Series) -> VarType: Classify one column. Drop NA first. Binary if n_unique == 2. If n_unique <= UNIQUE_THRESHOLD, check if values are sequential integers using float(v).is_integer() check (handles CSV float loading) and range(min, max+1) comparison -> ORDINAL if yes, CATEGORICAL if no. Otherwise CONTINUOUS. All-NA column returns CONTINUOUS.
       - detect_all_types(df: pd.DataFrame, pheno_cols: list[str]) -> dict[str, VarType]: Apply detect_type to each phenotype column. Return dict.
       - apply_overrides(type_map: dict[str, VarType], override_path: str | None) -> dict[str, VarType]: If override_path is None, return as-is. Otherwise read CSV with columns (variable_name, forced_type), update type_map. Validate forced_type is a valid VarType; log warning and skip invalid entries.

    2. Create tests/test_type_detector.py with behavior tests. Write tests RED first, then implement GREEN.

    Important: The sequential integer check MUST handle float-stored integers. Use `all(float(v).is_integer() for v in sorted_vals)` pattern. Also handle edge case where values like (0, 1) are both binary AND sequential -- binary check (n_unique == 2) takes precedence since it comes first.
  </action>
  <verify>
    <automated>cd /Users/bhargav/ai-coding/ABCD-PHEWAS && pytest tests/test_type_detector.py -x -v</automated>
  </verify>
  <done>All type detector tests pass. VarType enum exported. Auto-detection handles binary, ordinal (including float-stored ints), categorical, and continuous. Override CSV overrides any auto-detected type.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite and install</name>
  <files>tests/test_loader.py, tests/test_type_detector.py</files>
  <action>
    1. Run the full test suite with coverage: `pytest tests/ --cov=src/abcd_phewas --cov-report=term-missing -v`
    2. Verify all tests pass and coverage is above 80% for loader.py and type_detector.py.
    3. Fix any failures or gaps. Ensure no import errors between modules.
    4. Verify config/domain_mapping.yaml is valid YAML by loading it: `python -c "import yaml; yaml.safe_load(open('config/domain_mapping.yaml'))"`
    5. Verify the package installs cleanly: `pip install -e ".[dev]"` (should already be done but confirm no errors).
  </action>
  <verify>
    <automated>cd /Users/bhargav/ai-coding/ABCD-PHEWAS && pytest tests/ --cov=src/abcd_phewas --cov-report=term-missing -v && python -c "import yaml; d = yaml.safe_load(open('config/domain_mapping.yaml')); assert len(d['domains']) >= 9; print(f'Domains: {len(d[\"domains\"])}')"</automated>
  </verify>
  <done>Full test suite green. Coverage > 80% on loader.py and type_detector.py. Domain YAML loads with 9 entries (8 domains + Other/Unclassified). Package installs without errors.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -v` passes with all tests green
- `pytest tests/ --cov=src/abcd_phewas --cov-report=term-missing` shows >80% coverage on loader.py and type_detector.py
- `python -c "from abcd_phewas.loader import load_and_merge; from abcd_phewas.type_detector import VarType"` imports without error
- `python -c "import yaml; yaml.safe_load(open('config/domain_mapping.yaml'))"` loads without error
</verification>

<success_criteria>
1. Package structure exists at src/abcd_phewas/ with __init__.py, config.py, loader.py, type_detector.py
2. All loader tests pass: inner merge, configurable columns, sentinel replacement, CRLI blocking, missingness, min-n filter
3. All type detector tests pass: binary, ordinal, categorical, continuous detection + override
4. Domain mapping YAML has 8 domains with exact regex patterns from R codebase
5. pyproject.toml has all dependencies and pytest config
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
